(function (factory) {
    if (typeof module === "object" && typeof module.exports === "object") {
        var v = factory(require, exports);
        if (v !== undefined) module.exports = v;
    }
    else if (typeof define === "function" && define.amd) {
        define(["require", "exports", "../verbs/special"], factory);
    }
})(function (require, exports) {
    "use strict";
    const verbs_special = require("../verbs/special");
    zip = { everyone: 'no one',
        everybody: 'nobody',
        someone: 'no one',
        somebody: 'nobody',
        always: 'never',
        away: 'back',
        into: 'out of',
        on: 'off',
        over: 'under',
        together: 'apart',
        up: 'down',
        in: 'out',
        for: 'against',
        'up for': 'up against',
        'out for': 'out against',
        'it in': 'it out',
        'on with': 'off with' };
    return (function () {
        var negate = verbs_special.negate || {};
        for (var k in zip) {
            negate[k] = zip[k];
        }
        for (var k in negate) {
            negate[negate[k]] = k;
        }
        return negate;
    })();
});
//# sourceMappingURL=negate.min.js.map