import { DNode } from '../core/interfaces';
import { MatchDetails } from './interfaces';
export interface OutletProperties {
    renderer: (matchDetails: MatchDetails) => DNode | DNode[];
    id: string;
    routerKey?: string;
}
export declare const Outlet: import("../core/interfaces").WNodeFactory<{
    properties: OutletProperties & import("../core/interfaces").WidgetProperties;
    children: DNode<any>[];
}>;
export default Outlet;
