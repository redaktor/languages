import { PatchOperation } from './Patch';
import { MutableState, Path, State } from '../Store';
export declare class ImmutableState<T = any> implements MutableState<T> {
    private _state;
    /**
     * Returns the state at a specific pointer path location.
     */
    get: <U = any>(path: Path<T, U>) => U;
    at: <U = any>(path: Path<T, U[]>, index: number) => Path<T, U>;
    path: State<T>['path'];
    apply(operations: PatchOperation<T>[]): PatchOperation<T>[];
    private setIn;
    private set;
}
